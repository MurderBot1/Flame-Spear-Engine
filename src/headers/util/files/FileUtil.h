#ifndef FileUtil_H
#define FileUtil_H

#include <expected>
#include <filesystem>
#include <cstdarg>

namespace FSE::Util::Files::FileUtil {
    enum FileReadErrors {

    };

    enum FileWriteErrors {

    };

    enum FileCreationErrors {

    };

    enum FileDeletionErrors {

    };
}

#endif