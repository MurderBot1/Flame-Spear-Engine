#ifndef PacketBuilder_H
#define PacketBuilder_H

namespace FlameSpearEngine::Networking::Packet {
    class PacketBuilder {
        public:
            PacketBuilder();
            ~PacketBuilder();
        private:
    };
}

#endif